# OpenCode 本地 AGENTS 约定

## 定位

- 本文件只记录长期有效的本地协作约束，重点是编码质量、Git 使用、中文表达、验证方式与用户变更保护。
- OpenCode / Oh-My-OpenAgent 的 agent、category、model、provider、MCP、plugin、hook、skill、command、fallback、并发、缓存与实验特性都属于运行时状态，不在本文件固化为长期事实。
- 需要判断运行时能力、配置是否生效或工具是否可用时，以官方文档、当前 schema、实际工具描述、当前生效配置、`opencode debug config`、`doctor --verbose` 或等价诊断结果为准。
- 适合写入 `opencode.json[c]`、`tui.json[c]`、`.opencode/`、skill 文档、command 模板、插件配置或独立模板的内容，不写进 `AGENTS.md`。
- 如果只是某次诊断输出、某个模型链、某个 provider 状态或一次性迁移结论，不得沉淀到本文件。

## 硬约束

- Thinking 使用中文；面向用户的回复默认使用简体中文，除非用户明确要求其他语言。
- 文档名称、文档正文、Git 提交信息、Git 备注、变更日志、发布日志、PR / Issue 文案、代码注释、业务日志与面向团队的说明默认全部使用简体中文。
- 文件统一使用 UTF-8；除非文件本身已有其他字符集约束，否则不得引入 GBK / ANSI。
- 优先复用仓库现有实现、命名、结构和约定；以最小安全改动完成任务，不为了“更优雅”主动偏离项目风格。
- 结论必须基于实际读取、搜索、诊断或验证结果；不得对未读代码、未验证行为或状态不明的能力作确定性判断。
- 遇到错误严禁盲猜修复，必须基于真实日志、报错上下文和局部代码读取闭环分析。
- 阅读代码坚持按需加载，先局部定位，再展开上下文，避免无意义的大范围整仓或整文件读取。
- 在引入新的第三方依赖、框架、MCP、外部服务或长期运行能力前，必须先检查项目依赖与配置；确需新增时必须征得用户同意。
- 除非用户明确要求，严禁主动执行破坏性操作、修改 Git 历史、推送远程或直接提交。
- 不得把密钥、OAuth token、cookie、API key 或可还原敏感信息写入 `AGENTS.md`、示例配置、提交信息、日志、文档或总结。
- 后端动态查询条件必须按当前 ORM / 查询构造器表达；在 SQLAlchemy 中优先组合显式条件、`and_` / `or_` 或查询构造，不套用 `:param IS NULL OR ...` / `CASE WHEN ...` 这类 JPQL 写法。

## 默认技术栈与架构

- 默认主要技术栈为 React 19、TypeScript、Vite 8、Ant Design、ECharts、ExcelJS、FileSaver、FastAPI、Pydantic、SQLAlchemy、Alembic、PostgreSQL、JWT、Vitest、React Testing Library、Playwright、pytest、FastAPI TestClient、Docker、nginx、uvicorn 与 pnpm；若项目显式采用其他栈，以项目实际配置为准。
- 默认整体架构是 React / Vite 前端、FastAPI 后端与 PostgreSQL 数据库；生产环境采用 Docker 多阶段构建，并由单镜像中的 nginx 提供前端静态资源、反向代理 `/api/v1` 到 uvicorn / FastAPI。
- 新增依赖前必须先检查现有 `package.json`、`pnpm-lock.yaml`、`pyproject.toml`、`requirements*.txt`、Dockerfile 与部署配置；能复用既有栈时，不引入同类替代库。
- 跨前后端改动必须同时核对 API 路径、请求 / 响应模型、认证方式、错误结构、数据库迁移、前端调用与测试覆盖，避免只改一端。

## 简体中文规范

- 文档文件名优先使用简体中文；若项目已有英文命名体系、工具链限制或跨语言协作要求，可沿用项目约定，但文档正文必须使用简体中文。
- 代码标识符、包名、类名、函数名、数据库字段、API 路径等工程标识遵循语言规范和项目既有约定，不为了中文化破坏可维护性。
- 代码注释必须使用简体中文，除非引用第三方协议字段、标准错误码、外部 API 原文或必须保留的英文术语。
- Git 提交标题、提交正文、标签说明、发布说明、变更日志、PR 标题与描述、Issue 标题与描述、代码评审意见默认使用简体中文。
- 应用日志、审计日志、迁移日志、脚本输出、排障记录与运行手册默认使用简体中文；若日志面向机器解析、外部平台规范或国际化产品文案，则按项目约定处理。
- 用户可见文案按产品需求和既有 i18n 方案处理；没有明确多语言要求时，新增中文项目内容优先使用简体中文。
- 不确定是否应保留英文时，优先保留必要英文术语，并用简体中文解释其含义或约束。

## 代码注释约束

- 默认不为显而易见的代码补充注释，禁止把代码逐行翻译成中文。
- 公共类、公共类型、模块入口、公共方法、对外函数、复用型 Hook 与关键代码片段，不能仅因“代码可读”而免于注释。
- 注释重点解释 `why / constraint / invariant / side effect`，而不是重复代码字面含义。
- 以下场景必须补充或维护注释：
  - 公共类、公共类型、模块入口：说明职责、适用范围和使用限制。
  - 公共方法、对外函数、复用型 Hook：说明入参约束、返回值语义和主要副作用。
  - 关键业务规则：说明状态流转、金额或配额计算、权限判断、兼容逻辑和数据修正规则。
  - 非直观实现：说明顺序依赖、性能优化、缓存策略、降级逻辑、特殊边界处理和看似多余但必要的分支。
  - 外部约束：说明第三方 API 限制、协议字段约定、历史兼容原因、数据格式陷阱和平台差异。
  - 易误改节点：说明魔法值、时序要求、不可删除的保护逻辑和隐含不变量。
- 注释应放在关键语句或逻辑块上方，保持简洁、准确、可维护。
- 禁止“初始化数据”“处理结果”“调用方法”这类没有解释价值的空洞注释。
- 修改关键逻辑时必须同步更新相关注释；结束实现前必须自检是否存在“看得出做什么，但看不出为什么这样做”的关键节点。

## 编码、测试与部署约束

- 前端默认使用 React 19 + TypeScript + Vite 8 + Ant Design；组件、Hook、状态管理和表单处理优先遵循项目现有模式，不额外引入 UI 框架或构建工具。
- ECharts 图表必须处理容器尺寸变化、实例销毁和数据更新边界；ExcelJS / FileSaver 导出必须注意浏览器端内存占用、文件名、MIME 类型和用户可见错误提示。
- 后端默认使用 FastAPI + Pydantic + SQLAlchemy + Alembic + PostgreSQL；接口模型、数据库模型、迁移脚本和业务逻辑必须保持一致，数据库结构变化必须同步 Alembic 迁移。
- JWT、数据库连接、跨域、上传下载、反向代理和环境变量都属于边界能力；不得在代码、文档、日志或示例中写入真实密钥、token、连接串或 cookie。
- 测试优先按项目脚本运行；前端优先使用 Vitest、React Testing Library 和 Playwright，后端优先使用 pytest 与 FastAPI TestClient。修复缺陷时优先补充能复现问题的最小测试。
- 包管理默认使用 pnpm；不要混用 npm、yarn 或 bun 生成锁文件，除非项目已经明确采用或用户要求。
- 部署默认面向 Docker 多阶段构建、nginx 静态资源服务与 `/api/v1` 反向代理、uvicorn 承载 FastAPI 的单镜像模式；修改部署相关文件时必须同时核对构建产物路径、nginx 配置、健康检查和环境变量注入方式。

## Git 与变更记录

- 开始修改前优先了解当前工作区状态；不得覆盖、回滚或整理用户已有变更，除非用户明确要求。
- 提交前必须检查 `git status`、`git diff` 和近期提交风格；只暂存本次任务相关文件。
- 未经用户明确要求，不得执行 `git commit`、`git push`、`git rebase`、`git reset --hard`、`git checkout --`、`git clean`、强制推送或修改远端状态的操作。
- 用户要求提交时，先给出简体中文提交信息草稿，并确认已暂存内容不包含密钥、临时文件、无关格式化或一次性诊断输出。
- 提交信息使用简体中文，推荐格式为“类型：简要说明”，例如“文档：优化 AGENTS 协作约束”。若项目已有约定，优先遵循项目约定。
- 变更日志、发布日志和迁移说明必须描述用户可感知变化、兼容性影响、验证方式和已知风险，不写空泛的“优化代码”“修复问题”。
- Git 备注、代码评审备注、排障记录应写明背景、原因、处理方式和验证结论，避免只记录命令或结果。
- 不把本地短期状态写成长期事实，例如当前分支名、某次诊断输出、临时 provider 状态或一次失败命令。

## 证据与执行偏好

- 能从仓库内已有代码、配置、schema、注释和本地文档回答的问题，不要先求助外部资料。
- 默认优先本地结构化工具，其次本地文本搜索和读取；只有在本地证据不足且问题依赖外部事实时，再升级到官方文档、定向 MCP 或外部检索型子代理。
- 搜索范围必须显式收敛；避免对根目录、`node_modules` 或其他大目录做无过滤搜索，除非已经限定到具体包、文件或 schema。
- 已有足够证据支持结论时停止追加检索；连续两轮没有新增信息时，应转入汇总或执行。
- 长链路任务中优先保留关键发现的简洁摘要，不把大段原始输出反复塞进上下文。
- 轻量任务优先单会话、本地工具与可验证的串行执行；仅当任务明显扩展为多步骤、高不确定性或跨模块问题时，再升级流程。
- 在运行 build、compile、test、doctor 等高成本验证前，先做低成本前置检查；若缺少依赖、命令、环境变量、权限或外部服务，应标记为环境阻塞并说明缺口。
- 同一验证命令若因同类原因失败，在未改变代码、配置、依赖或环境前不得重复运行。
- 项目上下文、现有代码、配置与已读文档足以支持下一步时，不要重复向用户确认。
- 需要用户确认时，优先一次性给出 2 到 3 个可选方案、影响与推荐项，避免拆成过多轮确认。

## OpenCode / Oh-My-OpenAgent 使用边界

- 本文件保留 OpenCode 相关说明的目的，只是防止把运行时状态误写成长期约束；不维护具体安装清单、模型映射、fallback 链、MCP 列表或工具可用性快照。
- 核对 OpenCode / Oh-My-OpenAgent 配置时，必须牢记“本地文件不等于最终生效结果”：配置可能来自全局配置、项目配置、环境变量、`.opencode/`、managed settings、schema 迁移与运行时注册的合并结果。
- 判断 skill、command、agent、subagent、category、plugin、MCP 或 custom tool 是否可用时，以当前工具描述、权限配置和诊断输出为准，而不是只看文件是否存在。
- OpenCode / Oh-My-OpenAgent 新旧命名、兼容路径和实验特性可能并存；不得把某个单一路径、单一命令或单次诊断结果写成永久真相。
- 修改 OpenCode 配置、skill、command、plugin、hook、MCP 或权限规则前，先确认官方发现规则、当前 schema 和当前生效配置；修改后必须用低成本诊断或 diff review 验证。

## OpenSpec 工作流

- OpenSpec 是可选的规范驱动工作流；项目已存在 OpenSpec 工件或既有流程时，优先沿用，不要绕开它另起约定。
- 当在当前目录开始新工作，且本地证据未显示该项目已完成 OpenSpec 初始化时，先询问用户是否需要初始化；仅在用户明确同意后执行 `openspec init`。
- 若已检测到 OpenSpec 已初始化，或仓库内已存在明确的 OpenSpec 结构与约束，不要重复执行 `openspec init`。
- 对于新需求定义、正式提案、规范更新、行为变更或变更归档等明显适合 spec-first 的任务，优先使用项目当前实际可用的 OpenSpec 入口、文件和流程推进。
- 使用 OpenSpec 时，以项目内现有工件、帮助信息和当前配置为准；不要把某一版命令层级、profile、workflow、模板或入口名称当作永久事实。

## 维护

- 仅在本地长期工作方式变化，或官方行为变更已影响协作策略时更新本文件。
- 若只是运行时能力变化，优先修改配置、模板、skill 文档或插件文档，并按当前可用状态与诊断结果处理执行路径。
- 更新 `AGENTS.md` 时优先做小范围、可解释、可回滚的文档改动。
- 完成更新后必须检查 diff，确认没有写入模型清单、provider 清单、token、一次性诊断输出或其他短期状态。
